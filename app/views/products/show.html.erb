<div id="AddedMsg">
<% @added_product.each do |added_product| %>
	<p>You just added <b><%= added_product.title %></b> to your wishlist! </p>
<% end %>

<% @user_wishes.each_with_index do |w, indx| %>
	<%= w.product_id %> / <%= params[:id] %> <br/>
<% end %>

<%= @found_product %>

<br/>

<p><a href="/wishlist/<%= @customer_id %>">View your wishlist &rarr;</a></p>

</div>